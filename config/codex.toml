# Trusted Codex CLI configuration used by the GitHub Action.
# This file is fetched from the pinned control commit before Codex starts.
#
# Use the built-in profile directly. Codex does not allow a custom profile to
# extend :danger-full-access, and this workflow is intentionally unattended.
default_permissions = ":danger-full-access"
approval_policy = "never"
